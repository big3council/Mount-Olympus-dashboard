/* ── FlagOrb.jsx ──────────────────────────────────────────────────────────
 * Placeholder stub — real implementation lands in a separate task.
 *
 * Contract (from spec): one pulsing orb per open Gaia flag rendered in the
 * top-right cluster of the Projects tab header. Click expands a popover with
 * project name, issue, flagged_to, resolve / dig-in actions. Disappears when
 * resolved. Corner is empty when clear.
 *
 * This stub renders nothing so ProjectsView can import it safely today.
 * Replace with real component; keep the default export signature the same.
 */
export default function FlagOrb() {
  return null;
}
